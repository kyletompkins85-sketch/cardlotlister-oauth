# Copy to .env and fill in (see cardmatch/README.md).
# Option A — Cloudflare Worker (same as workflows)
WORKER_BASE_URL=
INTERNAL_API_KEY=

# Option B — Supabase PostgREST (full search if Worker env is unset)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
